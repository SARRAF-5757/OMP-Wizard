{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Debug Project",
            "type": "lldb",
            "request": "launch",
            "preLaunchTask": "build debug executable",
            "program": "${workspaceFolder}/build/omp-wizard_debug",
            "args": [],
            "stopOnEntry": false,
            "cwd": "${workspaceFolder}"
        }
    ]
}